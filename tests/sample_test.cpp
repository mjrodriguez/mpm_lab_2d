// Placeholder to confirm the Catch2 test harness is wired up correctly.
// Replace/extend with real unit tests (INTERPOLATION, ELASTOPLASTIC, TOOLS, ...).
#include <catch2/catch_test_macros.hpp>

TEST_CASE("Test harness is wired up", "[sample]") {
    REQUIRE(1 + 1 == 2);
}
